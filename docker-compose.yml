version: "3.3"

services:
  api:
    image: ptc:latest
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - src/.env
    ports:
      - "8000:8000"
    volumes:
      - ./src:/src
