overrides:
  vite>rollup: ">=4.59.0"
  "picomatch@>=4.0.0 <4.0.4": ">=4.0.4"
  "yaml@>=2.0.0 <2.8.3": ">=2.8.3"
  "serialize-javascript@<7.0.5": ">=7.0.5"
  # Keep each target within the patched major line — the vulnerable ranges are
  # all in-major, and an unbounded ">=x" lets pnpm jump a major (e.g. js-yaml 5.x
  # dropped types.merge, breaking @redocly/openapi-core used by gen:types).
  "brace-expansion@>=2.0.0 <2.1.2": ">=2.1.2 <6"
  "js-yaml@>=4.0.0 <4.3.0": ">=4.3.0 <6"
  "fast-uri@<3.1.4": ">=3.1.4 <5"
  "esbuild@>=0.27.3 <0.28.1": ">=0.28.1 <0.29"

# Postinstall build scripts. These ship prebuilt platform binaries via optional
# deps, so their build scripts are not needed (matches prior pnpm 10 behavior
# where they were ignored and the build + tests passed).
allowBuilds:
  "@swc/core": false
  esbuild: false
