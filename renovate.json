{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "labels": ["dependencies"],
  "schedule": ["before 9am on monday"],
  "timezone": "UTC",
  "packageRules": [
    {
      "matchUpdateTypes": ["minor", "patch"],
      "automerge": true,
      "description": "Auto-merge minor and patch updates"
    },
    {
      "matchManagers": ["pep621"],
      "groupName": "Python dependencies",
      "description": "Group all Python dependency updates"
    },
    {
      "matchManagers": ["npm"],
      "groupName": "Node dependencies",
      "minimumReleaseAge": "3 days",
      "description": "Group all Node.js updates; wait 3d so packages clear pnpm's minimumReleaseAge (24h) cutoff and CI/automerge can go green without a manual rebase"
    },
    {
      "matchPackageNames": ["react", "react-dom", "@types/react", "@types/react-dom"],
      "groupName": "React",
      "description": "Keep React packages in sync"
    }
  ]
}
